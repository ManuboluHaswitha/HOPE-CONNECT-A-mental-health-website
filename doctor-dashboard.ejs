<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Doctor Dashboard</title>
    <style>
        /* Overall Page Styling */
        body {
            font-family: 'Arial', sans-serif;
            background-color: #e3f2fd;
            /* Light blue background */
            margin: 0;
            padding: 0;
            display: flex;
            align-items: center;
            /* Aligns items vertically at the center */
            justify-content: center;
            /* Aligns items horizontally at the center */
            height: 100vh;
            /* Ensures the body takes up full viewport height */
        }

        /* Dashboard Container */
        .dashboard-container {
            text-align: center;
            background: white;
            padding: 40px;
            border-radius: 12px;
            box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.2);
            width: 50%;
            max-width: 600px;
            /* Ensures it doesn't grow too large on big screens */
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
        }

        /* Welcome Heading */
        h1 {
            font-size: 2.5rem;
            font-weight: bold;
            color: #1976D2;
            margin-bottom: 30px;
        }

        /* Links Styling */
        .options {
            width: 100%;
            display: flex;
            flex-direction: column;
            gap: 20px;
        }

        .options a {
            text-decoration: none;
            background: #1976D2;
            color: white;
            padding: 15px;
            font-size: 1.2rem;
            font-weight: bold;
            border-radius: 6px;
            width: 80%;
            max-width: 300px;
            text-align: center;
            transition: background 0.3s ease, transform 0.2s ease, box-shadow 0.3s ease;
            display: flex;
            justify-content: center;
            flex-direction: column;
            align-items: center;
        }

        /* Hover Effects */
        .options a:hover {
            background: #1565C0;
            transform: scale(1.05);
            box-shadow: 0px 4px 15px rgba(0, 0, 0, 0.2);
        }

        /* Responsive Design for Mobile */
        @media (max-width: 768px) {
            .dashboard-container {
                width: 80%;
            }

            .options a {
                width: 90%;
            }
        }
    </style>
</head>

<body>
    <div class="dashboard-container">
        <h1>Welcome, Doctor!</h1>
        <div class="options">
            <a href="/doctor/pending-appointments">View Pending Appointments</a>
            <a href="/doctor/set-availability">Set Availability</a>
            <a href="/logouttheri">Logout</a>
        </div>
    </div>
</body>

</html>