<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Appointment Confirmation</title>
    <style>
        /* Background same as quiz page */
        body {
            font-family: 'Arial', sans-serif;
            background-color: #f0f4f8;
            margin: 0;
            padding: 0;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            height: 100vh;
        }

        /* Container Styling */
        .container {
            background-color: #ffffff;
            padding: 30px;
            border-radius: 12px;
            width: 80%;
            max-width: 500px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
            text-align: center;
        }

        /* Heading */
        h1 {
            font-size: 2rem;
            color: #1976D2;
        }

        /* Message */
        .message {
            font-size: 1.2rem;
            color: #333;
            margin-top: 10px;
        }

        /* Back Button */
        .btn-back {
            display: inline-block;
            margin-top: 20px;
            padding: 10px 15px;
            background-color: #1976D2;
            color: white;
            text-decoration: none;
            border-radius: 6px;
            transition: background 0.3s ease, transform 0.2s ease;
            font-size: 1.1rem;
        }

        .btn-back:hover {
            background-color: #1565C0;
            transform: scale(1.05);
        }
    </style>
</head>

<body>

    <div class="container">
        <h1>Appointment <%= action %>!</h1>
        <p class="message">Your decision has been recorded successfully.</p>

        <a href="/doctor/pending-appointments" class="btn-back">Back to Pending Appointments</a>
    </div>

</body>

</html>